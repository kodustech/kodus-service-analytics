services:
  kodus-service-analytics:
    image: ${IMAGE_NAME}
    container_name: ${CONTAINER_NAME}
    volumes:
      - log_volume:/app/logs
    logging:
      options:
        max-size: "200m"
        max-file: "10"
    ports:
      - "${PORT}:${PORT}"
    environment:
      - ENV=homolog
      - NODE_ENV=homolog
    env_file:
      - .env.qa
    networks:
      - shared-network
    restart: unless-stopped
    build:
      context: .
      dockerfile: DockerFiles/Dockerfile.qa

volumes:
  log_volume:

networks:
  shared-network:
    external: true 